// Injected at build time by Vite's define (telecine/lib/util/buildViteConfig.ts).
// Absent in library builds and browsertests; always guard with typeof.
declare const __EF_TELEMETRY_ENABLED__: boolean;
