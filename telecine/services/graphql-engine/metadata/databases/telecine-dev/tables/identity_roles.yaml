table:
  name: roles
  schema: identity
is_enum: true
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: roles
  custom_root_fields: {}
array_relationships:
  - name: invites
    using:
      foreign_key_constraint_on:
        column: role
        table:
          name: invites
          schema: identity
