---
meta:
  - title: Set Image Duration
  - name: description
    content: Control whether images expand to fill parent or use fixed duration
headers:
    Cache-Control: no-cache
---

## Set Image Duration

**Problem:** You need to control how long an image displays - either expanding to fill its parent timegroup or using a fixed duration.

**Solution:** Use the `duration` attribute. When omitted or empty, the image expands to fill its parent. When set to a timestring (e.g., `"5s"`), the image uses that exact duration.

**Expanding to fill parent (default):**

<Demonstration layout="horizontal" sourceVisible alwaysShowSource enablePreview enableFilmstrip enableFitScale>
  <Timegroup mode="fixed" duration="5s" className="w-[960px] h-[540px] bg-black overflow-hidden">
    <Image className="w-full h-full object-contain object-center absolute top-0 left-0" src="https://assets.editframe.com/bridge.jpg"/>
  </Timegroup>
</Demonstration>

The image expands to fill the 5-second timegroup duration.

**Fixed duration:**

<Demonstration layout="horizontal" sourceVisible alwaysShowSource enablePreview enableFilmstrip enableFitScale>
  <Timegroup mode="contain" className="w-[960px] h-[540px] bg-black overflow-hidden">
    <Image duration="5s" className="w-full h-full object-contain object-center absolute top-0 left-0" src="https://assets.editframe.com/bridge.jpg"/>
  </Timegroup>
</Demonstration>

The image has a fixed 5-second duration, and the timegroup adapts to contain it.

Use fixed duration when creating slideshows or when you need precise control over image display time. Use expanding duration when you want images to adapt to their container's duration.

**Related:** Understand the architecture behind this behavior in the [Temporal Behavior](/docs/elements/image/explanation/temporal-behavior) explanation.


