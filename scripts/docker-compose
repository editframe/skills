#!/usr/bin/env bash
set -e

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

# Always delegate to telecine's docker-compose script
# It handles worktree detection and finding all compose files
"${SCRIPT_DIR}/../telecine/scripts/docker-compose" "$@"

