/**
 * Injected at build time by tsdown (production: "https://editframe.com")
 * and by @editframe/vite-plugin at dev/test time (local server origin).
 */
declare const __EF_DEFAULT_API_HOST__: string;
