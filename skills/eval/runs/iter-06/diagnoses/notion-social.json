[
  {
    "taskId": "notion-social",
    "feedbackItem": {
      "dimension": "motion",
      "severity": "major",
      "observation": "Motion uses standard CSS keyframes with fixed durations. The 'collapse-blocks' and 'expand-blocks' animations don't actually create the visual unification promised — blocks appear in a grid but don't visibly converge from scattered positions.",
      "improvement": "Use --ef-progress to drive the actual position interpolation of blocks from their scattered Scene 1 positions to the unified grid. The convergence should be continuous, not a scene cut. Consider addFrameTask for procedural block movement paths."
    },
    "skillFile": "skills/skills/brand-video-generator/references/composition-patterns.md",
    "currentText": "## Progress-Driven Animation\n\n`--ef-progress` is a CSS variable that holds the current playback progress (0–1) for any timegroup. It updates every frame. This makes it possible to drive any CSS property as a pure function of time — without keyframes.\n\n```html live\n<ef-timegroup mode=\"fixed\" duration=\"5s\" class=\"w-[720px] h-[200px] bg-slate-900 flex flex-col items-center justify-center gap-4 p-8\">\n  <div class=\"w-full bg-slate-700 rounded-full h-3 overflow-hidden\">\n    <div class=\"h-full bg-blue-400 rounded-full transition-none\"\n      style=\"width: calc(var(--ef-progress) * 100%)\"></div>\n  </div>\n  <ef-text class=\"text-white text-2xl\">Processing your request</ef-text>\n</ef-timegroup>\n```\n\n**What motion adds:** The progress bar is not decorating a static message — it is the message. The viewer watches time pass as a spatial event. Any numeric or proportional concept can be represented this way.",
    "cause": "missing",
    "proposedChange": "Add after the existing Progress-Driven Animation section:\n\n---\n\n## Position Interpolation (Convergence/Divergence)\n\nWhen elements must move from scattered positions to unified positions (or vice versa), use `--ef-progress` to interpolate between start and end coordinates. This creates continuous motion rather than a scene cut.\n\n```html live\n<ef-timegroup mode=\"fixed\" duration=\"3s\" class=\"w-[720px] h-[400px] bg-slate-900 relative\">\n  <!-- Each block interpolates from scattered start to grid end -->\n  <div class=\"absolute w-16 h-16 bg-blue-400 rounded\"\n    style=\"\n      --start-x: 50px; --start-y: 300px;\n      --end-x: 200px; --end-y: 150px;\n      left: calc(var(--start-x) + (var(--end-x) - var(--start-x)) * var(--ef-progress));\n      top: calc(var(--start-y) + (var(--end-y) - var(--start-y)) * var(--ef-progress));\n    \"></div>\n  <div class=\"absolute w-16 h-16 bg-blue-400 rounded\"\n    style=\"\n      --start-x: 600px; --start-y: 50px;\n      --end-x: 300px; --end-y: 150px;\n      left: calc(var(--start-x) + (var(--end-x) - var(--start-x)) * var(--ef-progress));\n      top: calc(var(--start-y) + (var(--end-y) - var(--start-y)) * var(--ef-progress));\n    \"></div>\n</ef-timegroup>\n```\n\n**What motion adds:** The convergence itself is the message — scattered things becoming unified. A keyframe animation with fixed durations cannot express this; the interpolation must be continuous and driven by progress. For more complex paths (curves, varied timing per element), use `addFrameTask`.\n\n**When to use this pattern:**\n- \"Chaos to order\" concepts (scattered → grid)\n- \"Unification\" concepts (fragments → whole)\n- Any motion where the *path* between states matters, not just the start and end",
    "rationale": "The model used fixed keyframe animations for 'collapse-blocks' because the documentation only showed --ef-progress for simple width/color changes, not for interpolating element positions between scattered and unified states."
  }
]