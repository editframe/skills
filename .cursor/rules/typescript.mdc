---
description: Use TypeScript # private syntax instead of underscore prefixes
globs: **/*.ts,**/*.tsx
alwaysApply: false
---

Always use `#` private syntax, never prefix with `_` underscore to indicate private.
